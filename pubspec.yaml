name: al_muhasib_althaki
description: "نظام محاسبي ومخزني متكامل للهاتف المحمول باللغة العربية"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  provider: ^6.1.1
  intl: ^0.19.0
  pdf: ^3.10.7
  printing: ^5.12.0
  share_plus: ^7.2.2
  image_picker: ^1.0.7
  barcode_scan2: ^4.2.4
  googleapis: ^11.4.0
  google_sign_in: ^6.1.6
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
